{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "prisma migrate deploy && node dist/src/main.js",
    "restartPolicyType": "NEVER",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
